# Rust build artifacts
/target/

# Swift Package Manager
.build/
.swiftpm/
# Package.resolved is deliberately committed (CR-1493): builds run with
# --force-resolved-versions, which refuses to resolve at all without it.

# macOS
.DS_Store

# IDE
.idea/
*.swp
*.swo
*.xcuserstate
xcuserdata/

# pczt-cli state (local wallet data)
# Note: wallet.json contains UFVK which is safe to share, but not secrets

# Build artifacts
__.SYMDEF
pczt-cli/libzcash_signer.a
macos-universal/
vendor/ZcashSigner.xcframework/

# Build artifacts
build/

# Config.plist symlink (created by xcodebuild wrapper, points outside repo)
Config.plist
